# User App

## Build Project
### Prerequisites
Build Api component
```
# path: user-app
cd api

# path: user-app/api
./mvnw package
```
### Docker
```
docker-compose build && docker-compose up -d
```

Access http://localhost:8000 to view dashboard